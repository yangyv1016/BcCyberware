.gradle/
.idea/
.tooling/
build/
out/
*.iml
run/

# 发布成品由 GitHub Release 托管，本地 release/ 中的说明与示例配置仍纳入版本控制。
release/*.jar
release/*.zip

# 图像生成阶段的高清原稿不进入 Git；可部署的 64x64 成品与 ZIP 纳入版本控制。
sample-pack/source-art/
